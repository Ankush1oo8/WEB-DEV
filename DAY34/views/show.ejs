<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>POST in detail</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <% if (post) { %>
        <p class="idx">Post id : <%= post.id %></p>
        <div class="post">
            <h3 class="username">@<%=post.username%></h3>
            <h4><%=post.content%></h4>
        </div>
    <% } else { %>
        <p>No post found.</p>
    <% } %>
    
</body>
</html>